# งานที่ต้องทำ — อ่านไฟล์นี้แล้วลงมือได้เลย

ทำทุกขั้นเองจนสุด ทำจนกว่าจะครบทุกคลิปหรือจนกว่าเจ้าของงานจะสั่งหยุด

## เรื่องการหยุดถาม — อ่านให้ชัด
**ห้ามหยุดถามเพื่อขออนุญาตทำงานปกติ** เช่น "ยิงคลิปต่อไหม" "commit เลยไหม" "ทำต่อไหม" ไม่ต้องถาม ทำเลย
ชนลิมิตก็รอแล้วทำต่อเอง ไม่ต้องรอใครปลุก

**แต่ให้หยุดแล้วรายงานทันที** ถ้าเจอ 5 อย่างนี้ เพราะมันแปลว่าสมมติฐานของงานพัง ไม่ใช่แค่ติดขัด
1. transcript หายหรือเสียหายเป็นวงกว้าง (เกิน 5 คลิป)
2. คลิปบน YouTube ถูกลบหรือเป็นส่วนตัว จนโหลดเสียงไม่ได้เกิน 5 คลิป
3. ผู้ช่วยสกัดออกมาผิดรูปแบบซ้ำๆ เกิน 3 คลิปติดกัน ทั้งที่ทำตามแม่แบบแล้ว
4. พื้นที่ดิสก์ไม่พอ หรือ GPU หน่วยความจำไม่พอจนถอดไม่ได้
5. อะไรก็ตามที่ต้องลบไฟล์นอกเหนือจาก "รื้อการ์ดเก่า" ตามข้อกำหนดในไฟล์นี้

## ขั้นเตรียม (ทำครั้งเดียว ข้ามได้ถ้าทำแล้ว)
1. `pip install yt-dlp pymupdf`
2. ก๊อปโฟลเดอร์ `_skill/research-book` ไปไว้ที่ `~/.claude/skills/research-book`
3. รัน `python scripts/restore_evidence.py` (โหลดคลังอัลกุรอานและหะดีษกลับมา ใช้เวลาสักครู่)
4. อ่าน `STATE.md` และ `HANDOFF.md` ให้จบ ในนั้นมีข้อตกลงทั้งหมดที่เจ้าของงานเคาะไว้แล้ว

## ขั้น 0: ถอดเสียงใหม่ด้วย GPU ก่อน (ทำก่อนสกัดการ์ด — สำคัญ)
ถ้าเครื่องคุณมีการ์ดจอ (GPU) ให้ทำขั้นนี้ก่อนเสมอ **มันไม่กินโควตา Claude เลย** และทำให้ทุกขั้นที่เหลือดีขึ้น
transcript ที่มีอยู่ตอนนี้เป็นซับอัตโนมัติของ YouTube ซึ่งอ่านรู้เรื่องแค่ราว 60% คำอาหรับผิดเกือบหมด เลขหน้า/เลขอายะฮ์เพี้ยน

A. `pip install torch --index-url https://download.pytorch.org/whl/cu124` แล้ว `pip install transformers accelerate librosa soundfile`
B. `python scripts/download_audio.py` — โหลดไฟล์เสียงทั้ง 114 คลิป ราว 10 GB (รันซ้ำได้ ข้ามที่มีแล้ว)
C. `python scripts/transcribe_gpu.py` — ถอดด้วย **Typhoon Whisper large v3** (ฝึกด้วยเสียงไทย 11,000 ชั่วโมง จัดการตัวเลขไทยดีกว่ามาก)
   รันทิ้งไว้ข้ามคืนได้ · มันจะพิมพ์บอกว่าเร็วกว่าเวลาจริงกี่เท่า · รันซ้ำได้ ข้ามคลิปที่ถอดแล้ว
D. `python scripts/promote_gpu_transcripts.py` — เอา transcript ใหม่มาเป็นตัวหลัก (ของเดิมสำรองไว้)
E. แล้วค่อยไปทำขั้น 5 เป็นต้นไป

**ถ้าเครื่องไม่มี GPU ให้ข้ามขั้น 0 ทั้งหมด** ใช้ transcript เดิมสกัดการ์ดไปเลย งานยังเดินได้ แค่คุณภาพด้อยกว่า

หมายเหตุ: ช่วงที่อาจารย์อ่านตัวบทอัลกุรอานเป็นภาษาอาหรับยาวๆ จะยังเพี้ยนอยู่ดี เพราะโมเดลฝึกด้วยเสียงไทย
ไม่เป็นไร เพราะตัวบทอาหรับเราจะดึงจากฐานข้อมูลมาตรฐานในขั้นตรวจหลักฐานอยู่แล้ว
สิ่งที่ดีขึ้นชัดคือภาษาไทย ชื่ออุละมาอ์ ชื่อหนังสือ และตัวเลข ซึ่งคือปัญหาส่วนใหญ่ของเรา

## งานหลัก: สกัดการ์ดประเด็นจากคลิปที่เหลือ (เหลือ 80 คลิป)
5. รัน `python scripts/next_batch.py 6` ดูว่าคลิปไหนยังไม่มีการ์ด
6. เอาแม่แบบใน `scripts/EXTRACT_PROMPT.md` มาแทนค่าทุก `{ช่อง}` ให้ครบ
   แล้ว spawn ผู้ช่วยด้วย Agent tool คลิปละ 1 ตัว **ใส่ `model: "opus"` ทุกตัว** รันขนาน 6 ตัว
   จด id คลิปที่กำลังทำลงใน `cards/_inflight.txt` กันยิงซ้ำ
7. ตัวไหนเสร็จ **ตรวจ 2 อย่างก่อนนับว่าใช้ได้**
   - ท้ายไฟล์มีหัวข้อ `## ไม่ทำการ์ด`
   - การ์ดใบสุดท้ายครอบถึงนาทีใกล้เคียงความยาวคลิป
   ขาดข้อใดข้อหนึ่ง = ผู้ช่วยตายกลางคัน ต้องรันคลิปนั้นใหม่ทั้งคลิป
   ครบทั้งสองข้อ = ลบ id ออกจาก `cards/_inflight.txt` แล้วยิงคลิปใหม่แทนทันที
8. วนแบบนี้จนกว่า `next_batch.py` จะบอกว่าเหลือ 0
9. ชนลิมิตแล้วให้ทำต่อทันทีที่รีเซ็ต ไม่ต้องรอใครสั่ง
   ผู้ช่วยที่ล้มเพราะลิมิต บางตัวเขียนไฟล์ทันแล้ว ให้ตรวจตามข้อ 7 ก่อนตัดสินว่าต้องทำใหม่ไหม
10. commit ทุกครั้งที่ได้การ์ดเพิ่มราว 5 คลิป (ข้อความ commit เป็นไทย) แล้ว **ลอง `git push`**
    - ถ้า push ผ่าน = เจ้าของงานเชิญคุณเป็นผู้ร่วมงานแล้ว ให้ push ทุกครั้งที่ commit ตั้งแต่นี้ไป
      เขาจะได้เห็นงานทยอยเข้าและดึงไปใช้ได้ทันทีโดยไม่ต้องรอจบ
    - ถ้า push ไม่ผ่าน (403 / permission denied) = ยังไม่ได้รับเชิญ ไม่ต้องพยายามอีก
      ให้ commit ในเครื่องต่อไปเรื่อยๆ แล้วตอนจบค่อยซิปโฟลเดอร์ `cards/` ส่งกลับ
      **อย่าหยุดงานเพื่อรอสิทธิ์** ทำต่อได้เลย

## ห้าม
- **ห้ามเริ่มเขียนหนังสือ** เจ้าของงานจะเป็นคนเคาะโครงเล่มเอง
- **ห้ามแก้หรือลบไฟล์การ์ดที่มีอยู่แล้ว** ใน `cards/`
- **ห้ามใช้ sonnet กับผู้ช่วยสกัดการ์ด** ทดสอบแล้วมันทิ้งหลักฐาน จับได้ 3 จาก 17 รายการ ตารางผลอยู่ท้าย `STATE.md`
- **ห้ามทิ้งประเด็นใดๆ ตอนสกัด** แม้จะลึกเกินไปสำหรับมุสลิมใหม่ ให้ติดป้าย "ลึกเกิน" แทน แล้วค่อยไปคัดตอนเขียนเล่ม
- **ห้ามปล่อยช่อง model ว่างตอน spawn ผู้ช่วย** เพราะมันจะไปหยิบรุ่นเริ่มต้นซึ่งอาจเป็น sonnet โดยไม่มีใครรู้ตัว

## ถ้าทำขั้น 0 แล้ว: รื้อการ์ดเก่าตามลำดับนี้
การ์ด 34 คลิปที่มีอยู่สกัดมาจากซับ YouTube ที่แย่กว่า `promote_gpu_transcripts.py` จะพิมพ์รายชื่อให้
**ไม่ต้องประเมินว่า "โควตาเหลือไหม" — วัดไม่ได้ ให้ไล่ทำตามลำดับนี้ไปเรื่อยๆ จนกว่าจะหมดหรือถูกสั่งหยุด**

- **เฟส 1** สกัด 80 คลิปที่ยังไม่มีการ์ดให้ครบก่อน ห้ามข้ามไปเฟส 2 จนกว่า `next_batch.py` จะบอกว่าเหลือ 0
- **เฟส 2** รื้อการ์ดเก่าใหม่ เรียงตามนี้ ทำทีละคลิปแบบเดียวกับเฟส 1
  1. ชุด 2020 ตอน 11–28 (เนื้อหาเตาฮีด ชิริก มลาอิกะฮ์ = แกนกลางของเล่ม สำคัญสุด)
  2. ชุด 2020 ตอน 1–10 (บทนำ ความสำคัญของการเรียน พิสูจน์พระเจ้า)
  3. ที่เหลือ
  วิธีรื้อ: ลบไฟล์การ์ดคลิปนั้นทิ้ง แล้ว `next_batch.py` จะเสนอคลิปนั้นขึ้นมาเอง
  ก่อนลบให้ commit ไว้ก่อนหนึ่งครั้ง จะได้กู้คืนได้ถ้าตัวใหม่ออกมาแย่กว่า
- ชนลิมิตระหว่างเฟสไหนก็ทำต่อจากตรงนั้นเมื่อรีเซ็ต ไม่ต้องเริ่มใหม่

## ส่งงานกลับ — อ่านให้ครบ อย่าส่งแค่การ์ด

**ของที่ต้องกลับมา 2 อย่าง ไม่ใช่อย่างเดียว**
1. `cards/` — การ์ดประเด็นทั้งหมด (ผลงานหลัก)
2. `sources/transcripts/` — transcript ที่ถอดใหม่ด้วย GPU **สำคัญมาก**
   เครื่องเจ้าของงานไม่มีการ์ดจอ สร้างชุดนี้เองไม่ได้ ถ้าไม่ส่งกลับคือหายถาวร
   ถ้าเผลอส่งแต่การ์ด งานตรวจหลักฐานกับงานเขียนหนังสือจะต้องกลับไปใช้ซับ YouTube ห่วยๆ เหมือนเดิม
3. ถ้ามี `sources/transcripts_gpu/` อยู่ด้วยก็ส่งมาเลย

**ของที่ไม่ต้องส่งกลับ**
- `sources/audio/` (ราว 10 GB โหลดใหม่ได้)
- `evidence/` ที่เป็นฐานอัลกุรอาน/หะดีษ (โหลดใหม่ได้ด้วย `restore_evidence.py`)
- `sources/raw/`, `sources/docs/*.pdf`

**ส่งยังไง**
- ถ้า `git push` ผ่าน: ไม่ต้องทำอะไรเพิ่มเลย ทั้งการ์ดและ transcript ถูกติดตามใน git อยู่แล้ว
  push ครั้งสุดท้ายให้ครบ แล้วบอกเจ้าของงานว่าเสร็จ
- ถ้า push ไม่ผ่าน: ซิป 2 โฟลเดอร์นี้ส่งไป → `cards/` + `sources/transcripts/` (รวมกันราว 40 MB)

**รายงานปิดงาน บอกให้ครบ 5 ข้อ**
1. สกัดการ์ดครบกี่คลิป จากทั้งหมดกี่คลิป · ได้การ์ดรวมกี่ใบ
2. ถอดเสียงใหม่ด้วย GPU ครบไหม ใช้โมเดลอะไร คลิปไหนถอดไม่ได้บ้าง
3. รื้อการ์ดเก่าไปแล้วกี่คลิป (เฟส 2) เหลือกี่คลิปที่ยังเป็นของเก่าจากซับ YouTube
4. คลิปไหนที่เสียงหรือซับแย่จนควรกลับไปฟังเสียงจริงตอนตรวจหลักฐาน
5. อะไรที่ติดค้าง ทำไม่ได้ หรือคุณไม่แน่ใจ — บอกตรงๆ ดีกว่าเงียบ
