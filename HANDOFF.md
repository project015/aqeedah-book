# คู่มือรับงานต่อ — หนังสือหลักศรัทธาสำหรับมุสลิมใหม่

สวัสดีครับ งานนี้คือการสกัดเนื้อหาจากคลิปบรรยายหลักศรัทธา 113 คลิป (223 ชั่วโมง) ของสำนักพิมพ์อัซซาบิกูน
เพื่อเอาไปเรียบเรียงเป็นหนังสือสำหรับคนที่เพิ่งรับอิสลาม **ตอนนี้ยังไม่ถึงขั้นเขียนหนังสือ ห้ามเริ่มเขียน**
สิ่งที่ต้องทำต่อคือ **สกัด "การ์ดประเด็น" จากคลิปที่เหลือ**

---

## ติดตั้งก่อนเริ่ม (ครั้งเดียว)

1. **Claude Code** พร้อมบัญชีที่มีโควตาเหลือพอ (นี่คือคอขวดจริงของงาน ไม่ใช่สเปกเครื่อง)
2. **Python 3.11+** แล้วรัน
   ```
   pip install yt-dlp pymupdf
   ```
3. **คัดลอกไฟล์สกิล** จากโฟลเดอร์ `_skill/research-book/SKILL.md` ในซิปนี้
   ไปวางที่ `C:\Users\<ชื่อคุณ>\.claude\skills\research-book\SKILL.md`
4. **กู้ไฟล์ฐานข้อมูลหลักฐานคืน** (ไม่ได้ใส่มาในซิปเพราะไฟล์ใหญ่) เปิด terminal ที่โฟลเดอร์นี้แล้วรัน
   ```
   python scripts/restore_evidence.py
   ```
   จะโหลดคลังหะดีษภาษาอาหรับ 9 ชุดกลับมาให้ ใช้เวลาไม่กี่นาที

---

## เริ่มทำงาน

เปิด Claude Code ที่โฟลเดอร์นี้ แล้วพิมพ์ประโยคเดียว

```
อ่าน STATE.md กับ HANDOFF.md แล้วสกัดการ์ดคลิปที่เหลือต่อจนครบ ห้ามเขียนหนังสือ
```

Claude จะรู้เองว่าต้องทำอะไร เพราะ `STATE.md` เก็บข้อตกลงทั้งหมดไว้แล้ว
ถ้าอยากสั่งเองทีละก้าว ให้ทำวนแบบนี้

1. `python scripts/next_batch.py 6` → บอกว่าคลิปไหนยังไม่มีการ์ด
2. เอา `scripts/EXTRACT_PROMPT.md` มาแทนค่า `{ID}` `{SERIES}` `{EP}` `{MIN}` `{TITLE}` `{SLIDES}` `{NCARD}`
   แล้วสั่ง Claude ให้ spawn ผู้ช่วย (Agent tool) **รุ่น opus** ทีละคลิป รันพร้อมกันได้ 6 ตัว
3. พอตัวไหนเสร็จ ให้ลบ id นั้นออกจาก `cards/_inflight.txt` แล้วยิงคลิปใหม่แทน
4. ทำจนกว่า `next_batch.py` จะบอกว่าเหลือ 0

---

## กฎเหล็ก 5 ข้อ (เจ้าของงานสั่งเอง ห้ามฝ่าฝืน)

1. **อย่าทิ้งอะไรเลย** ทุกประเด็นที่อาจารย์พูดต้องมีการ์ด แม้จะเป็นเรื่องลึกเกินไปสำหรับมุสลิมใหม่
   ให้ติดป้ายระดับ "ลึกเกิน" ไว้ แล้วค่อยไปคัดตอนเขียนหนังสือ ไม่ใช่คัดทิ้งตอนนี้
2. **ช่องหลักฐานสำคัญที่สุด** ทุกอายะฮ์ ทุกหะดีษ ทุกคำพูดอุละมาอ์ที่อาจารย์ยก ต้องลงในช่อง "หลักฐาน"
   ซับมันเพี้ยนจนอ่านไม่ออกก็ไม่เป็นไร ให้จดว่าได้ยินเป็นอะไรกับเดาว่าน่าจะเป็นอะไร แยกกันไว้
   ถ้าไม่จด = หลักฐานหายถาวร เพราะไม่มีใครรู้ว่าต้องไปตรวจอะไร
3. **ใช้ผู้ช่วยรุ่น opus เท่านั้น** ทดสอบมาแล้ว sonnet ทิ้งหลักฐาน (จับได้ 3 จาก 17) ผลอยู่ท้าย STATE.md
4. **เลขสากล 1 2 3 เท่านั้น** ห้ามเลขไทย
5. **ห้ามแตะไฟล์การ์ดของผู้ช่วยตัวอื่น** ผู้ช่วยแต่ละตัวเขียนไฟล์ของตัวเองใบเดียว

---

## สิ่งที่ทำเสร็จแล้ว อย่าทำซ้ำ

- ดึงซับและทำ transcript ครบ 113 คลิป อยู่ใน `sources/transcripts/`
- ดึงเอกสารสไลด์ประกอบจาก Drive 46 ไฟล์ ดึงข้อความแล้วอยู่ใน `sources/docs/*.md`
- เตรียมฐานข้อมูลตรวจหลักฐานครบ (อัลกุรอานอาหรับ + แปลไทย, หะดีษ 9 ชุด, หะดีษแปลไทย 846 บท)
- สกัดการ์ดแล้ว 29 คลิป อยู่ใน `cards/`

## ยังไม่ได้ทำ

- สกัดการ์ดอีก 84 คลิป ← **งานของคุณ**
- รวมการ์ดตามหัวข้อ (`python scripts/merge_topics.py` รันได้เลยเมื่อการ์ดครบ)
- ตรวจหลักฐานทีละชิ้น
- เขียนหนังสือ ← เจ้าของงานจะเป็นคนเคาะเอง

---

## เครื่องมือที่มีให้แล้ว

| คำสั่ง | ทำอะไร |
|---|---|
| `python scripts/next_batch.py 6` | บอกคลิปถัดไปที่ยังไม่มีการ์ด |
| `python scripts/quran.py 2:255` | ดึงตัวบทอัลกุรอานอาหรับ + แปลไทย |
| `python scripts/quran.py --th "อสุจิ"` | ค้นอัลกุรอานด้วยคำแปลไทย |
| `python scripts/hadith.py "نص"` | ค้นหะดีษภาษาอาหรับใน 9 ชุดหลัก |
| `python scripts/dorar.py "نص"` | เช็คสถานะหะดีษจาก dorar.net |
| `python scripts/merge_topics.py` | รวมการ์ดทั้งหมดจัดกลุ่มตามหัวข้อ |

---

## ส่งงานกลับ

ซิปโฟลเดอร์ `cards/` ทั้งอัน แล้วส่งกลับให้เจ้าของงาน (Ponraphat) พอ
ไม่ต้องส่งอย่างอื่นกลับ เพราะไฟล์อื่นไม่ได้ถูกแก้
